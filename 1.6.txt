to find the last no of tuple or highest primary key value in the table:
=> select max(dept_id) from department; #select max(pk_value) from tablename;

to create sequence we will take employee table 

create sequence emp_sq start with 1 increment by 1;

create table emp (emp_id int primary key , name varchar2(10));

insert into emp (emp_id,name) values (emp_sq.nextval,'1');
insert into emp (emp_id,name) values (emp_sq.nextval,'12');
insert into emp (emp_id,name) values (emp_sq.nextval,'13');
insert into emp (emp_id,name) values (emp_sq.nextval,'14');

to add new columns PINCODE to the table (emp)
=> alter table emp add pincode int not null;

#we cant add the pincode with constraint with not null 







to drop data
=> truncate

to change the field name:
=> alter table emp rename column name to emp_name;

to rename the table
=> rename emp to employee 


##tuple= row
##attribute =colms